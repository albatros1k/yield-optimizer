version: "2"

networks:
  graph-node_main:
    external: true


services:
  ui:
    build: .
    container_name: vaults-ui
    environment:
      - NODE_ENV=production
    ports:
      - "4173:4173"
    networks:
       graph-node_main:
         ipv4_address: 172.22.0.100






