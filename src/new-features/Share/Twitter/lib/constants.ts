const hashtags = ['odysea', 'odysea_protocol', 'apy', 'pool', 'market', 'tvl'];

const odyseaTwitterName = 'Odysea_protocol';

const relatedKeys = ['crypto', 'odysea'];

export { hashtags, odyseaTwitterName, relatedKeys };
